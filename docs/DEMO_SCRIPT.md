# Demo Video — Script & Shot List (target: 3:00)

*Read naturally, click along. Before recording: server running with FOUNDRY badge ON, Ollama running, browser at localhost:8765, one terminal visible. Close notifications.*

## SHOT 0 — Hook (0:00–0:15) · Assistant view
**SHOW:** Command Center landing, model badge visible.
**SAY:** "AI assistants are powerful — but forgetful. Every session, your project context dies. Scope creep sneaks in. Tokens burn on work nobody asked for. This is KISS Studio: durable memory, scope governance, and every AI decision cited and auditable. One system — submitted across all three tracks."

## SHOT 1 — Three brains, one engine (0:15–0:45) · Assistant view
**DO:** Type "Why was the podcast spin-off parked?" → press **⚖ Compare**.
**SAY:** "Same prompt, three brains: deterministic offline, a local open-weight model through Ollama, and gpt-4o on Azure Foundry — with timings. Notice every answer cites the project's own files. The model is swappable; the governance never moves."
**POINT OUT:** the 👍 prefer button — "every preference I click builds a fine-tuning dataset."

## SHOT 2 — The scope gate (0:45–1:15) · Project Ops view
**DO:** Active project: creator-launch. Scope Gate → type "let's build the merch store" → Check.
**SAY:** "I'm launching a YouTube channel. I just asked for a merch store. The Assessment Agent reads MY product vision, classifies it Tier 3, BLOCKS it, and parks it in the decision log with a revisit trigger. That's rule R-4: capture it, don't build it. The whole left panel — board, context cost BEFORE I spend it, an evidence-seeded health score — comes from plain Markdown files any agent can read."

## SHOT 3 — Create anything, governed (1:15–2:00) · Creative Studio
**DO:** Creative chat: "survey me: a board-game café brand" → answer the tailored questions fast → Generate.
**SAY:** "The intake adapts to what you're building. My answers become project memory — the off-limits answer goes straight into the risk policy. Watch the asset checks: the governor ACCEPTs the kit the model proposed for *this kind* of project, and budget caps are enforced before any compute is spent."
**DO:** Click an asset → ✏️ AI → "make it warmer, add steam rising from a mug."
**SAY:** "Chat-edits art, grounded in the project's own tone. Original kept, edit queued for human verification."

## SHOT 4 — The manager's view (2:00–2:25) · Teams mock
**DO:** Open m365-track3/mock-ui/teams-mock.html.
**SAY:** "Track 3: a declarative agent for Microsoft 365 Copilot. These Adaptive Cards render the engine's REAL trace files — scope decisions with the why, team readiness from the certification programme, and a verification-debt brake. Governance where managers already live."

## SHOT 5 — Proof, not vibes (2:25–2:50) · terminal
**DO:** `python evals.py` (pre-run if slow; show 10/10) then `python query.py "prerequisite" --source ontology`.
**SAY:** "Ten governance evals from our cold-run test kit — ten out of ten, rerunnable on every model tier. And one query reaches every intelligence layer: Foundry IQ's cited knowledge, Fabric IQ's ontology rules, Work IQ's signals, and the full reasoning traces."

## SHOT 6 — Close (2:50–3:00) · Assistant view
**SAY:** "Local-first, Azure-amplified, governed by its own methodology — this repo's decision log is the system eating its own popsicle. KISS Studio: ship useful work, keep the human better off, make the agent prove it."

## Backup beats (if a take dies)
- Onboard any folder: + folder → navigate → "Onboard THIS folder" (two clicks, files untouched)
- 📅 → CALENDAR: board tasks exported to Outlook as .ics
- Popsicle Index: evidence line under the sliders
